<clickhouse>
  <zookeeper>
    {% for host in groups['keeper'] %}
    <node>
      <host>{{ hostvars[host]['ansible_default_ipv4']['address'] }}</host>
      <port>{{ clickhouse_keeper_tcp_port }}</port>
    </node>
    {% endfor %}
  </zookeeper>
</clickhouse>
